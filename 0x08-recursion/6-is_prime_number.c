#include "main.h"

/**
 * is_prime_number -Entry point
 * description: Prime numbers
 * @n: Parameter
 * Return: Return prime number Success
 */

int is_prime_number(int n)
{
	if (n % n == 0)
		return (1);
	return (0);
}
