#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _strdup -Entry point
 * @str: String
 * Return: Return String Success
 */
char *_strdup(char *str)
{
	char newstr;
	int i;

	if (str == NULL)
	{
		return (NULL);
	}

	newstr == str;
	newstr = char * +malloc(sizeof(str));

	if (newstr[i] < newstr)
	{
		return (NULL);
	}

	return (newstr);
}
